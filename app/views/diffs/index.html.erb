<p id="notice"><%= notice %></p>

<h1>Diffs</h1>

<table>
  <thead>
    <tr>
      <th>Content</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @diffs.each do |diff| %>
      <tr>
        <td><%= diff.id %></td>
        <td><%= link_to 'Show', diff %></td>
        <td><%= link_to 'Edit', edit_diff_path(diff) %></td>
        <td><%= link_to 'Destroy', diff, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Diff', new_diff_path %>
